node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# OpenCode MCP auth
.opencode/*-auth.json

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Impeccable skill artifacts
.impeccable/

# Logo generator previews
/scripts/.tmp/

# Social media assets (Pexels) - regenerados con `bun run stock fetch`
/static/social/

# Fuentes Onest copiadas por `bun run fonts` (legacy — regeneradas con las 3 familias del sistema)
/static/fonts/

# Tokens de marca autogeados desde DESIGN.md (regenerados con `bun run tokens`)
/video/src/brand.generated.ts

# Poster A3 — logs residuales del servidor `bun run poster`
/poster.log
/poster.err.log
